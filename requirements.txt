torch>=2.4
torchvision
numpy
pillow
pyyaml
einops
transformers
datasets
huggingface_hub
safetensors
pyarrow
xxhash
opencv-python-headless
imagehash
scipy  # Tier-D whitekey_alpha (scipy.ndimage.binary_propagation)
fastapi
uvicorn
httpx
# Frozen text encoder (Qwen3-VL-Embedding), used offline for prompt conditioning
sentence-transformers>=5.4
qwen-vl-utils>=0.0.14

# Local VLM annotator server (optional): scripts/serve_vlm.py
# The annotator itself only needs httpx (above) when using the http backend.
# vllm
